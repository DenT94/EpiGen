# Copy this file to .env and fill in real values. .env is gitignored.

# Biohub hackathon API (biohub.ai) -- ESMFold2, ESMC, ESMC SAE, binder design.
# Register at day 1 per CLAUDE.md; exact var name TBD until docs/portal confirmed.
BIOHUB_API_KEY=

# Anthropic API key for the stage-4 explanation agent (epigen.pipeline.explain).
ANTHROPIC_API_KEY=
